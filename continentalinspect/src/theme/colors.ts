/**
 * Default palette (dark). Prefer `useTheme().colors` in screens for light/dark support.
 */
import { darkPalette, type AppColors } from '@/theme/palettes';

export const colors = darkPalette;

export type Colors = AppColors;
