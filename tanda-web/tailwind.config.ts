import type { Config } from 'tailwindcss';

/**
 * Tailwind v4: theme colors live in globals.css (@theme inline).
 * Do not use <alpha-value> here — v4 handles opacity via color-mix().
 */
const config: Config = {};

export default config;
